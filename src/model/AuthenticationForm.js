/**
 * dotCMS REST API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AuthenticationForm model module.
 * @module model/AuthenticationForm
 * @version 3
 */
class AuthenticationForm {
    /**
     * Constructs a new <code>AuthenticationForm</code>.
     * @alias module:model/AuthenticationForm
     * @param userId {String} 
     * @param password {String} 
     */
    constructor(userId, password) { 
        
        AuthenticationForm.initialize(this, userId, password);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, userId, password) { 
        obj['userId'] = userId;
        obj['password'] = password;
    }

    /**
     * Constructs a <code>AuthenticationForm</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AuthenticationForm} obj Optional instance to populate.
     * @return {module:model/AuthenticationForm} The populated <code>AuthenticationForm</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AuthenticationForm();

            if (data.hasOwnProperty('userId')) {
                obj['userId'] = ApiClient.convertToType(data['userId'], 'String');
            }
            if (data.hasOwnProperty('password')) {
                obj['password'] = ApiClient.convertToType(data['password'], 'String');
            }
            if (data.hasOwnProperty('rememberMe')) {
                obj['rememberMe'] = ApiClient.convertToType(data['rememberMe'], 'Boolean');
            }
            if (data.hasOwnProperty('backEndLogin')) {
                obj['backEndLogin'] = ApiClient.convertToType(data['backEndLogin'], 'Boolean');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], 'String');
            }
            if (data.hasOwnProperty('country')) {
                obj['country'] = ApiClient.convertToType(data['country'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AuthenticationForm</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AuthenticationForm</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AuthenticationForm.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['userId'] && !(typeof data['userId'] === 'string' || data['userId'] instanceof String)) {
            throw new Error("Expected the field `userId` to be a primitive type in the JSON string but got " + data['userId']);
        }
        // ensure the json data is a string
        if (data['password'] && !(typeof data['password'] === 'string' || data['password'] instanceof String)) {
            throw new Error("Expected the field `password` to be a primitive type in the JSON string but got " + data['password']);
        }
        // ensure the json data is a string
        if (data['language'] && !(typeof data['language'] === 'string' || data['language'] instanceof String)) {
            throw new Error("Expected the field `language` to be a primitive type in the JSON string but got " + data['language']);
        }
        // ensure the json data is a string
        if (data['country'] && !(typeof data['country'] === 'string' || data['country'] instanceof String)) {
            throw new Error("Expected the field `country` to be a primitive type in the JSON string but got " + data['country']);
        }

        return true;
    }


}

AuthenticationForm.RequiredProperties = ["userId", "password"];

/**
 * @member {String} userId
 */
AuthenticationForm.prototype['userId'] = undefined;

/**
 * @member {String} password
 */
AuthenticationForm.prototype['password'] = undefined;

/**
 * @member {Boolean} rememberMe
 */
AuthenticationForm.prototype['rememberMe'] = undefined;

/**
 * @member {Boolean} backEndLogin
 */
AuthenticationForm.prototype['backEndLogin'] = undefined;

/**
 * @member {String} language
 */
AuthenticationForm.prototype['language'] = undefined;

/**
 * @member {String} country
 */
AuthenticationForm.prototype['country'] = undefined;






export default AuthenticationForm;

